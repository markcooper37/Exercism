#if !defined(SERIES_H)
#define SERIES_H
#include <vector>
#include <string>

namespace series {
    std::vector<std::string> slice(std::string digits, int length);
}  // namespace series

#endif // SERIES_H